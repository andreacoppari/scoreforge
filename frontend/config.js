// For Cloudflare Pages, replace "" with your HTTPS worker origin (no trailing slash).
// Keep it empty when using the bundled Docker Compose setup, which proxies /api locally.
window.SCOREFORGE_API_URL = "";
