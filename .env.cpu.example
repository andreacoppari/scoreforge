DEVICE=cpu
TRANSCRIPTION_ENGINE=basic-pitch
MUSCRIPTOR_MODEL=large
# Required only if TRANSCRIPTION_ENGINE=muscriptor. Accept the gated model license first.
HF_TOKEN=
MAX_UPLOAD_MB=800
MAX_CONCURRENT_JOBS=1
JOB_TTL_HOURS=24
DEMUCS_MODEL=htdemucs
ALLOWED_ORIGINS=http://localhost:8080
